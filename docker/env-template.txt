# ========================================
# SCM 项目环境变量配置模板
# ========================================
# 使用说明：
# 1. 在项目根目录或 docker 目录创建 .env 文件
# 2. 复制下面的内容到 .env 文件
# 3. 根据实际情况修改配置值
# 4. 不要将 .env 文件提交到版本控制

# ========================================
# 应用基础配置
# ========================================

NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# ========================================
# 数据库配置（生产环境请修改密码）
# ========================================

DATABASE_URL=postgresql://scmuser:scmpassword@postgres:5432/scm
POSTGRES_DB=scm
POSTGRES_USER=scmuser
POSTGRES_PASSWORD=scmpassword

# ========================================
# MQTT 配置
# ========================================

MQTT_BROKER=mqtt://mqtt
MQTT_PORT=1883
MQTT_WS_PORT=8883
MQTT_USERNAME=
MQTT_PASSWORD=

# ========================================
# 前端配置
# ========================================

VITE_API_URL=http://localhost:3000

# ========================================
# Docker 端口映射
# ========================================

ADMIN_PORT=8080
SERVER_PORT=3000
POSTGRES_PORT=5432
MQTT_TCP_PORT=1883
MQTT_WS_PORT=8083
MQTT_DASHBOARD_PORT=18083

